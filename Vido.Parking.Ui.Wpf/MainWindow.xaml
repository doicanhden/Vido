<Window
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:Views="clr-namespace:Vido.Parking.Ui.Wpf.Views"
  xmlns:ViewModels="clr-namespace:Vido.Parking.Ui.Wpf.ViewModels"
  x:Class="Vido.Parking.Ui.Wpf.MainWindow" Loaded="Window_Loaded"
  Title="Bãi xe Vido" MinHeight="633" MinWidth="961" WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <DataTemplate DataType="{x:Type ViewModels:LaneViewModel}">
      <Views:LaneView BorderThickness="1" BorderBrush="Black" DataContext="{Binding}" FontSize="20"/>
    </DataTemplate>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Button HorizontalAlignment="Left" Margin="2" Width="30" Height="30" Content="L" Command="{Binding ShowLaneConfigsCommand}"/>
    <ItemsControl Grid.Row="1" Margin="0,0,0,0" ItemsSource="{Binding Lanes}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <UniformGrid Rows="1" IsItemsHost="True"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
    </ItemsControl>

    <StatusBar Grid.Row="2" Height="20">
      <TextBlock Text="{Binding Status}"/>
    </StatusBar>
  </Grid>
</Window>
